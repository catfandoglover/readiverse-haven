
import React from "react";
import BookshelfCarousel from "../BookshelfCarousel";

const EpistemologyContent: React.FC = () => {
  return <BookshelfCarousel queryKey="epistemology-books" />;
};

export default EpistemologyContent;
